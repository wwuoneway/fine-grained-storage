# Copy to .env and set for your machine. Used by scripts/run_study_all.sh.
#
# SPACK_SETUP: absolute path to Spack's setup-env.sh (the file you `source`).
# SPACK_ENV:   the Spack environment that provides ROOT 6.40
SPACK_SETUP="/path/to/spack/share/spack/setup-env.sh"
SPACK_ENV="your-root-env"
